

const inputter = document.getElementById("input").value.trim();
const resutplex = document.getElementById("resultOrg");

const getMasker(inputer) {
    
    if (typeof inputer !== "string" || !inputer.includes(@)) {
        throw new Error("Invalid Email! Please enter a new one");
    }

}